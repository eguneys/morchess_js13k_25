{
  "name": "morchess_js13k_25",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist && rimraf dist.zip",
    "zip": "cd tools && optipng.exe -o7 ../dist/assets/*.png && cd ../dist && 7z a ../dist.zip -mx9 && cd .. && cd tools && advzip.exe --recompress --shrink-extra ../dist.zip",
    "dev": "vite --port=3000",
    "build": "pnpm clean && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "advzip-bin": "^2.0.0",
    "rimraf": "^6.0.1",
    "terser": "^5.43.1",
    "typescript": "~5.8.3",
    "vite": "^7.1.2",
    "vite-plugin-imagemin": "^0.6.1"
  }
}
